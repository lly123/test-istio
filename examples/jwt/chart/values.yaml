# Default values for chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 2

fullnameOverride: my-ws

image:
  repository: simple-web-server
  tag: latest
  pullPolicy: Never

service:
  type: ClusterIP
  port: 8080
